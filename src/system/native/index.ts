import mediaDevices from './core/mediaDevices'
import screen from './core/screen'

import support, { canIUse } from './support'

export {
  canIUse,
  support,

  screen,
  mediaDevices
}

export default {
  canIUse,
  support,

  screen,
  mediaDevices
}
